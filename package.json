{
  "homepage": "https://github.com/Parag479/services",
  "name": "services-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-icons": "^5.2.1",
    "react-router-dom": "^6.0.0",
    "react-scripts": "^5.0.0",
    "web-vitals": "^4.2.2"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "react-scripts --openssl-legacy-provider build",
    "start": "react-scripts --openssl-legacy-provider start",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "generate-component": "node generateComponent.js"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "gh-pages": "^6.1.1"
  }
}
